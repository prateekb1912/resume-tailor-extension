# OS
.DS_Store

# n8n workflow exports — kept local, not committed (root-anchored so it doesn't
# swallow .github/workflows/)
/workflows/

# Python / server
server/.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
server/.env
.env
.vscode/

# Local release artifacts
/dist/
